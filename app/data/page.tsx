"use client";import Link from "next/link";import{useState}from"react";export default function Data(){const[done,setDone]=useState(false);function clear(){if(!confirm("Remove NutClue measurements, clues and preferences stored in this browser? This cannot be undone."))return;["nutcluePrefs","nutclueClues","nutclueLogs"].forEach(k=>localStorage.removeItem(k));setDone(true)}return <main className="legal"><Link href="/">← NutClue</Link><p className="eyebrow">MY DATA</p><h1>Your device. Your clues. Your control.</h1><p>NutClue's current tracking features keep your entries in this browser. There is no NutClue account or cloud health record behind these tools.</p><h2>Remove local NutClue data</h2><p>This removes measurements, clue history and saved preferences from this browser only. It cannot remove information you previously copied or shared elsewhere.</p><button className="dangerAction" onClick={clear}>Clear my local NutClue data</button>{done&&<p role="status"><b>Local NutClue data cleared.</b></p>}<h2>Before changing devices</h2><p>Local entries do not automatically move to another device or browser. NutClue is not a medical record and should not be your only copy of information you need to retain.</p></main>}